export const metadata = {
  title: "AfterCare — Open the app",
  description:
    "Money, housing, school, health, paperwork — figured out step by step, free, with no judgment.",
};

export default function AppLayout({ children }) {
  return children;
}
